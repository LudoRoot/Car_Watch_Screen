<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="controllers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="innovationcenter.car_watch.controllers.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="innovationcenter.car_watch.controllers.CallActivity" />
        <activity android:name="innovationcenter.car_watch.controllers.RadioActivity" />
        <activity android:name="innovationcenter.car_watch.controllers.ConfActivity" />
        <activity android:name="innovationcenter.car_watch.controllers.MovieActivity" />
        <activity android:name="innovationcenter.car_watch.controllers.NavActivity" />
        <activity android:name="innovationcenter.car_watch.controllers.BTActivity" />
        <activity android:name="innovationcenter.car_watch.controllers.AudioFxActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

</manifest>